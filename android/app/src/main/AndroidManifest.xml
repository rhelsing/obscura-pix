<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- FCM silent push receiver. Service receives data-only wake-up pings
           from obscura-server and asks the kit to drain queued envelopes,
           then posts a hardcoded generic local notification. -->
      <service
        android:name=".ObscuraMessagingService"
        android:exported="false">
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>

      <!-- Default notification channel (Android 8+) for any system-fallback FCM
           notification. Must match the channel id NotificationHelper creates +
           posts on — otherwise the system would silently auto-create a stub
           "Miscellaneous" channel and our posts would lose IMPORTANCE_HIGH /
           heads-up behavior. Server only sends silent pushes today so this
           is dormant in practice, but it's a landmine to leave mismatched. -->
      <meta-data
        android:name="com.google.firebase.messaging.default_notification_channel_id"
        android:value="obscura_messages"
        tools:replace="android:value" />
    </application>
</manifest>
